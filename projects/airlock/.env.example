# npm run demo:live loads .env automatically. Exported variables take priority.
# One key runs both the isolated range and recorded cloud browsers.
SOLARI_API_KEY=slr_live_...

# Optional. Keep generated evidence outside the repository if preferred.
AIRLOCK_ARTIFACTS_DIR=./artifacts
